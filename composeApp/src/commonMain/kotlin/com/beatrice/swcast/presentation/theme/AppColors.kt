package com.beatrice.swcast.presentation.theme

import androidx.compose.material3.lightColorScheme

val swColorScheme =
    lightColorScheme(
        primary = gold,
        surface = black,
        onSurface = gold,
        background = black,
        onBackground = gold,
        error = red,
        onError = white
    )
